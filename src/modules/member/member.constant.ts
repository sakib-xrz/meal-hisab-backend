/**
 * Sensible defaults & limits for the Member module.
 */
export const MEMBER_CONSTANTS = {
  MIN_NAME_LENGTH: 2,
  MAX_NAME_LENGTH: 80,
  MAX_EMAIL_LENGTH: 120,
  MAX_ROOM_NO_LENGTH: 32,
} as const;
